import { SafeAreaView } from "react-native";
import PhoneBookBox from "@/components/phonebooks/PhoneBookBox";

export default function HomeScreen() {
  return (
      <SafeAreaView>
        <PhoneBookBox />
      </SafeAreaView>
  );
}
