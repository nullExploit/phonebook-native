import { SafeAreaView } from "react-native";
import PhoneBookAdd from "@/components/phonebooks/PhoneBookAdd";

export default function add() {
  return (
    <SafeAreaView>
      <PhoneBookAdd />
    </SafeAreaView>
  );
}
